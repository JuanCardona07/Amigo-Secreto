PORT=4000
FRONTEND_ORIGIN=http://localhost:3000
# Opcional en local (sin ella se usa un archivo en server/data/state.json).
# En Render, ponla para que el estado sobreviva a los reinicios del servicio.
REDIS_URL=
